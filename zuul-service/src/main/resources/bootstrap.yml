eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
management:
  security:
    enabled: false
security:
  basic:
    enable: false
server:
  port: 8084
spring:
  application:
    name: zuul-service
zuul:
  host:
    socket-timeout-millis: 30000
  routes:
    rest:
      serviceId: rest-service
